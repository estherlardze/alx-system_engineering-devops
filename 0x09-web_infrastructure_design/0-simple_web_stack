A user enters the URL (www.foobar.com) into the browser's search bar. The browser checks its cache to see if there is an IP address to the URL. 
 Otherwise, the operating system checks its cache to see if it has the IP  address. If your operating system doesn't have it, the resolver will query the root servers, 
 and the root servers will do it for you 
 Provide an IP address for your TLD server. The resolver caches this IP address and queries the TLD server for the authority's IP address. 
 Nameservers and resolvers cache this IP address. Finally, the authoritative name server gives the resolver the IP address www.foobar.com. 
 Store the IP address and return it  to the browser within milliseconds. This will allow your browser to go to the www.foobar.com.

 1. Server 
 A server is a computer or system that provides resources, data, services, or programs to other computers, known as clients, over a network
 2. ** Domain name **
Domain name is unique name give to a computer which has n IP Adress of a specific computer on the internet.
From the picture a client use a domain name called www.foobar.com which has an IP Adress of 8.8.8.8

3. ** The type of DNS record www is in www.foobar.com is a CNAME **
CNAME is a type of resource record in Domain Name System

4. ** The role of the web Server **
The role of the web server is to send or request information through the Application files, App Servers from the Database.

5. ** Role of Application Server **
The role of the application server is to send data back and forth to the web server and the database.

6. ** Role of the Database **
The role of the database is to store data, that can be modifies, update, or request from a clinet.

7. The is using HyperText Transfer Protocol (HTTP).

8. SPOF is Single Point of Failure
SPOF is any non-reduadant part of a system that, if dyfunctional, would cause the entire system to fail.

9. If a system is having a downtime, it diffiicult to access it, so what the system does is to send a message of to the main pasge so whoever access will know what is going on.

10. In computing, whenever is a heavy request of data or access to a files, there will be a great impact on the network which will cause unexpected traffic.

 
